package Models;

public class Gerant extends Client {
    public Gerant(int idClient, String name, String email, String password, int groupId) {
        super(idClient, name, email, password, groupId);
    }
}
